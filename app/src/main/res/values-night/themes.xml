<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ToDoList" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/blue</item>
        <item name="colorSecondary">@color/blue</item>
        <item name="colorAccent">@color/blue</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>
    <!-- Define custom ActionBar style -->
    <style name="MyActionBar" parent="Widget.MaterialComponents.ActionBar.Solid">
        <item name="android:background">@android:color/transparent</item> <!-- Transparent background -->
        <item name="titleTextStyle">@style/MyActionBarTitleText</item>
    </style>

    <!-- Define custom text appearance for ActionBar title -->
    <style name="MyActionBarTitleText" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@color/white</item> <!-- Set your desired color here -->
        <item name="android:textSize">20sp</item>
    </style>
</resources>